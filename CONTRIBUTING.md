# Contributing

Create a focused branch, add tests for parser or rule changes, run `ruff check .` and `pytest`, and document new supported syntax or limitations. Findings must be deterministic, evidence-linked, and conservatively worded.
